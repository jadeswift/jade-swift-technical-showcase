import React from "react";
import {Box} from "@mui/material";
import {FCWithChildren} from "../common/fc-with-children";

export const Albums: FCWithChildren = () => (
    <Box>
        This is the albums page
    </Box>
);
